// Lets TypeScript's editor resolve global stylesheet imports like `import './globals.css'`.
// (Next.js compiles these fine at build time — this only quiets the editor squiggle.)
declare module '*.css'
